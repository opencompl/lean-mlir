; Prove: x + y = y + x  (commutativity of addition)
(set-logic QF_BV)
(declare-const k Int)
(declare-const x (_ BitVec k))
(declare-const y (_ BitVec k))
(assert (not (= (bvadd x y) (bvadd y x))))
(check-sat)
